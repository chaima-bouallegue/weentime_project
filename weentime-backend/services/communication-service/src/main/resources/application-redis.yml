spring:
  data:
    redis:
      url: ${REDIS_URL:redis://localhost:6379}

communication:
  redis:
    enabled: ${REDIS_ENABLED:false}
    topic: ${REDIS_COMMUNICATION_TOPIC:communication.realtime}

# Test commit: validating Docker Hub credentials fix in pipeline
