server:
  port: 8761

spring:
  application:
    name: discovery-service

  cloud:
    config:
      enabled: false

eureka:
  instance:
    hostname: localhost

  client:
    register-with-eureka: false
    fetch-registry: false

  server:
    enable-self-preservation: false