@startuml
title Diagramme de séquence — Consultation des responsables RH
autonumber

actor "Administrateur" as Acteur
boundary "Interface Angular" as Angular
control "API Gateway" as Gateway
control "Organisation Service" as OrganisationService
database "Base de données PostgreSQL" as DB

Acteur -> Angular : Accéder au module Responsables RH
Angular -> Gateway : Demander la liste des responsables RH

ref over Acteur, Gateway
S'authentifier()
end ref

Gateway -> OrganisationService : Transmettre la demande

ref over Gateway, OrganisationService
Contrôler les accès (ADMIN)
end ref

OrganisationService -> DB : Récupérer responsables RH et entreprises
DB --> OrganisationService : Retourner les données
OrganisationService --> Gateway : Retourner la liste
Gateway --> Angular : Transmettre la liste
Angular --> Acteur : Afficher la liste

@enduml
